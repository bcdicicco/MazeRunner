using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScoreManager : MonoBehaviour
{






   // Dictionary<string, Dictionary<string, string>> playerScores;

    //void start()
    //{

    //}

    //void Init()
    //{
    //    if (playerScores != null)
    //        return;

    //    playerScores = new Dictionary<string, Dictionary<string, string>>();
    //}
    //public string GetTime(string name, string scoreType)
    //{
    //    Init();

    //    if (playerScores.ContainsKey(name) == false)
    //    {
    //        return "";
    //    }

    //    if (playerScores[name].ContainsKey(scoreType) == false)
    //    {
    //        return "";
    //    }

    //    return playerScores[name][scoreType];
    //}

    //public void SetTime(string name, string scoreType, string time)
    //{
    //    Init();
    //    if (playerScores.ContainsKey(name) == false)
    //    {
    //        playerScores[name] = new Dictionary<string, string>();
    //    }

    //    playerScores[name][scoreType] = time;
    //}

}
